# BatCave parser + fixtures

Contiene parser TypeScript, fixtures HTML e test Vitest.

```bash
npm install
npm test
```

Nota: le fixtures sono versioni ridotte degli HTML che mi hai mandato, ma mantengono i selettori/dati necessari al parser. Puoi sostituirle con gli HTML integrali mantenendo gli stessi nomi.
